# Godot 4 editor cache — auto-generated, never commit
.godot/

# Export artefacts
export.cfg
export_presets.cfg
*.pck
!docs/*.pck
*.apk
*.aab
*.ipa
*.dmg
*.exe

# Large binary model assets — baked into export .pck at build time, keep local only
assets/models/

# macOS
.DS_Store
